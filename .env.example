# Flask Configuration
SECRET_KEY=your-secret-key-here
FLASK_ENV=development
FLASK_DEBUG=true

# Database Configuration
DB_USERNAME=your-db-username
DB_PASSWORD=your-db-password
DB_HOST=localhost
DB_NAME=quested

# OpenAI Configuration
OPENAI_API_KEY=your-openai-api-key

# Email Configuration
# Choose one: 'smtp' or 'gmail_api'
EMAIL_METHOD=smtp

# SMTP Configuration (for EMAIL_METHOD=smtp)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password  # Use Gmail App Password, not regular password

# Gmail API Configuration (for EMAIL_METHOD=gmail_api)
# Place your service account credentials.json file in the project root

# Testing
TEST_EMAIL=test@example.com